# 状态导出与监控钩子

## 目标

为业务提供池状态可观测性:实例状态变迁的结构化暴露,便于接入现有监控(如 opencode2api 的探针)。

## 改动点

- 状态导出:Instances() 之外增加轻量计数快照(各状态实例数/重播累计次数/拨号累计与失败计数)
- Logger 之外不引入回调 API(遵循 overview "不做"边界,仅被动查询式)

## 验收点

- 计数器原子读零开销
- 数据足以支撑业务侧健康面板(Normal 数/重播频率/失败率)
